// Generated by CoffeeScript 1.9.3
(function() {
  module.exports = function() {
    return window.addEventListener('popstate', function(e) {
      var data, hostName, href, labelList, text;
      data = history.state;
      href = location.href;
      hostName = location.hostname;
      labelList = Q('[data-action="labelList"]');
      this.isShowing = false;
      if (data) {
        document.title = data.title + +'-Sharlock\'s blog';
      }
      switch (false) {
        case !(href.indexOf('list') > 0):
          if (data && data.other && data.other.index) {
            text = data.content;
            if (labelList.Q('.active')) {
              labelList.Q('.active').removeClass('active');
            }
            if (labelList.QA('li')[data.other.index]) {
              labelList.QA('li')[data.other.index].addClass('active');
              TweenLite.to(labelList.Q('.line'), 0.35, {
                top: (data.other.index * 77) + 'px',
                ease: Bounce.easeOut
              });
            }
          }
          break;
        case !(href.indexOf('show') > 0):
          if (data) {
            text = data.other.card;
          }
          break;
        case href !== hostName:
          Q('.avatar').click();
          labelList.Q('.line').addClass('hidden');
      }
      if (text) {
        return require('./articleListAnimation.js')(D('articleList'), text);
      }
    });
  };

}).call(this);

//# sourceMappingURL=historyChange.js.map
