// Generated by CoffeeScript 1.9.3
(function() {
  var loadComments;

  loadComments = require('./loadComments.js');

  module.exports = function(contentBlock, catId, content) {
    var backButton, card, cardScope, cards, i, item, len;
    card = this;
    cardScope = this.parentNode;
    cards = cardScope.QA('.content-item');
    for (i = 0, len = cards.length; i < len; i++) {
      item = cards[i];
      if (item !== card) {
        cardScope.removeChild(item);
      }
    }
    contentBlock.Q('.summary').innerHTML = content;
    TweenLite.from(contentBlock.Q('.summary'), 1, {
      height: 20,
      opacity: 0,
      ease: Bounce.easeOut
    });
    prettyPrint();
    loadComments.call(contentBlock);
    backButton = CE('a');
    backButton.className = 'ui button teal back';
    backButton.href = '/list/' + catId + '.html';
    backButton.setAttribute('data-title', card.data('cat-name'));
    backButton.setAttribute('data-method', 'back');
    backButton.innerHTML = '返回';
    card.appendChild(backButton);
    return TweenLite.from(backButton, 0.5, {
      opacity: 0,
      rotation: "360deg",
      x: 100,
      y: -100
    });
  };

}).call(this);

//# sourceMappingURL=showAnimation.js.map
