// Generated by CoffeeScript 1.9.3
(function() {
  module.exports = function(href, title, cst, data, fn, other) {
    var self;
    self = this;
    if (isFunction(data)) {
      fn = data;
    }
    if (href) {
      return queryData(href, (getType(data) === 'object' ? data : {}), function(res) {
        var content, state;
        if (title) {
          document.title = title + '-Sharlock\'s blog';
        }
        content = res['data']['content'];
        state = {
          content: content,
          id: href,
          title: title
        };
        if (other) {
          state.other = other;
        }
        history.pushState(state, title, href);
        if (isFunction(fn)) {
          fn.call(self, content);
        }
        if (!cst) {
          self.isShowing = false;
          return require('./articleListAnimation')(D('articleList'), content);
        }
      });
    }
  };

}).call(this);

//# sourceMappingURL=getContent.js.map
